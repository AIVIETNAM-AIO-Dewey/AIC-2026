schema_version: "1.0"
run:
  run_id: frame-extraction-v1
seed: 2026

video_id: L21_V001
limit: 10
jpeg_quality: 2
fallback_timestamps_s: [0, 1, 2, 3, 4]

adaptive_sampling:
  short_shot_max_s: 3.0
  cadence_s: 1.5
  max_frames_per_shot: 10
  dedupe_tolerance_s: 0.5

shot_detection:
  backend: pytorch

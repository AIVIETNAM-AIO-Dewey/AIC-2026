# Direct runtime dependencies mirror the core pins in pyproject.toml. Cloud setup
# installs this list with --no-deps; runners import the local src tree directly.
# transformers/tokenizers/huggingface-hub were raised from 4.48.3/0.21.0/0.28.1 so the
# Siglip2 architecture is available; it only exists from the v4.49.0-SigLIP-2 tag onward.
# The bump stays inside the 4.x line to limit the blast radius on the SAM/DAM stage,
# which must be smoke-tested again by its owner. Nothing else needed to move.
huggingface-hub==0.36.2
accelerate==1.3.0
jsonschema==4.23.0
numpy==1.26.4
pillow==11.1.0
protobuf==5.29.3
pydantic==2.10.6
# Vector index for scene embeddings. Client only; the Qdrant server is a separate
# native binary or container, never a Python dependency.
qdrant-client==1.19.0
pyyaml==6.0.2
safetensors==0.5.2
sentencepiece==0.2.0
tokenizers==0.22.2
tqdm==4.67.1
transformers==4.57.6

# PyTorch is intentionally supplied by the Colab/Kaggle GPU image. The preflight
# check rejects CPU-only installations before model load.

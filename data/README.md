# Competition data

Nội dung thư mục này không được commit. Mount hoặc tải dữ liệu cuộc thi vào
`data/raw` thông qua storage riêng tư, giữ nguyên file gốc và checksum. Derived
frames/audio không đặt tại đây; ghi vào `AIC_ARTIFACT_ROOT`.
